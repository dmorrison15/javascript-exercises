const fibonacci = function() {
  const fibSequence = [1, 1];
  for(let i = 2; i < a; i++) {
    fibSequence.push(fibSequence[i - 1] + fibSequence[i - 2]);
  }
  return fibSequence[a - 1];
};

// Do not edit below this line
module.exports = fibonacci;
